Django>=3.2,<6.0  # 支援 Django 3.2 到 5.x 的一個合理範圍
django-simple-captcha>=0.5.17 # 基於之前遇到的問題和穩定性考慮
Pillow>=9.0.0 # Pillow 的一個較新且穩定的版本
# 注意：以上版本號是建議值，您可以根據實際測試和兼容性需求進行調整。
# 如果項目在特定版本下開發和測試，建議固定到那些版本。
# 例如：
# Django==5.2.3
# django-simple-captcha==0.6.2
# Pillow==11.2.1
#
# 要生成您當前環境中精確的依賴列表（在激活虛擬環境並安裝所有包之後），請運行：
# pip freeze > requirements.txt
